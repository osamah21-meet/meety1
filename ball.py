from turtle import Turtle
int new_x=0
class ball(Turtle):
	def ___init___(self,r,dx,dy,color):
		Turtle.___init___(self)
		self.r=r
		self.dx=dx
		self.dy=dy
		self.shape("Circle")
		self.color=color
		self.size=r/10
	def move(screen_width,screen_height)
		current_x=self.xcor()
		current_y=self.ycore()
